# Copy this file to ".env" and adjust for your local MySQL setup.
# (.env is gitignored so your real credentials are never committed.)

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=mypassword123
DB_NAME=nextstep

# Port the Express API listens on.
PORT=4000
